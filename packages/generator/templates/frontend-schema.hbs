export const schema = {
  fields: [
{{#schemaFields}}
    {
      name: "{{ name }}",
      type: "{{ type }}",
      label: "{{ label }}",
      required: {{ required }},
    }{{^isLast}},{{/isLast}}
{{/schemaFields}}
  ],
} as const;
